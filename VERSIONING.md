# 버전 관리 규칙

스킬은 라이브러리가 아니라 문서에 가깝습니다.
그래서 "버저닝이 굳이 필요한가"라는 질문이 나오는데, 이 저장소는 필요하다고 판단했습니다.
그 근거와 규칙을 적어둡니다.

## 왜 버저닝하나

### 1. 출력 스키마가 사실상의 계약이다

`company-diligence`는 RED / AMBER / GREEN / UNKNOWN 4단계로 판정하고,
리포트를 정해진 섹션 구조로 냅니다.
사용자는 여러 회사를 조회해 결과를 나란히 비교합니다.
판정 등급 체계나 섹션 구조가 조용히 바뀌면 **이전에 뽑은 리포트와 비교가 불가능해집니다.**
버전이 없으면 사용자는 자기 리포트 두 개가 왜 다른지 알 방법이 없습니다.

### 2. 스킬끼리 서로를 입력으로 받는다

```
jd-analyzer  ──jd-analysis.md──┐
                               ├──> application-strategy ──> resume-customizer
company-diligence ─company-report.md─┘
```

`application-strategy`는 앞 두 스킬의 산출물 형식에 의존합니다.
앞 스킬의 출력 형식이 바뀌면 뒤 스킬이 깨집니다. 이건 실제 API 의존성과 같은 성질입니다.

### 3. 외부 API 스펙에 묶여 있다

스크립트가 국세청, 국민연금, DART API를 호출합니다.
국민연금 API는 이미 엔드포인트에 `V2`가 붙어 있습니다. V3가 나오면 스크립트가 깨집니다.
어느 스킬 버전이 어느 API 버전에 대응하는지 기록이 없으면 원인 추적이 어렵습니다.

### 4. 설치 방식 때문에 사용자가 자기 버전을 모른다

패키지 매니저가 아니라 `cp -r` 이나 심볼릭 링크로 설치합니다.
`npm ls` 같은 걸로 확인할 수 없으므로, **버전을 파일 안에 적어두지 않으면 확인할 방법이 없습니다.**
그래서 각 `SKILL.md` frontmatter에 버전을 명시합니다.

```yaml
metadata:
  version: "0.1.0"
  repo: https://github.com/useful-skills/job-application-skills
```

이 방식은 이미 배포되고 있는 다른 스킬 묶음에서도 쓰는 패턴입니다.

## 저장소 단위 버전, 스킬 단위 아님

이 저장소의 스킬 4개는 **파이프라인으로 결합되어 있으므로 한 버전으로 함께 움직입니다.**

스킬마다 따로 버전을 매기면 이런 질문이 생깁니다.
"jd-analyzer 1.2.0과 application-strategy 0.9.0이 호환되나?"
조합 수만큼 호환성 매트릭스를 관리해야 하는데, 얻는 게 없습니다.

반대로 서로 독립적인 스킬을 모아둔 저장소라면 스킬 단위 버저닝이 맞습니다.
**결합도가 기준이지 취향이 아닙니다.**

## 무엇이 MAJOR이고 무엇이 PATCH인가

문서 중심 프로젝트에서 semver가 모호해지는 지점이라 명시해둡니다.
판단 기준은 하나입니다. **사용자의 기존 결과물이나 워크플로가 깨지는가.**

### MAJOR (X.0.0) - 깨진다

- 판정 등급 체계 변경 (예: 4단계를 3단계로 축소, 등급 이름 변경)
- 리포트 섹션 구조 변경 (번호나 제목이 바뀌어 이전 리포트와 대조 불가)
- 스킬 이름 변경 또는 제거
- 스킬 간 주고받는 산출물 형식 변경
- 입력 요구사항 추가 (전에는 회사명만으로 됐는데 이제 사업자번호 필수)

### MINOR (0.X.0) - 늘어난다

- 새 검증 항목이나 데이터소스 추가
- 새 스크립트 추가
- 새 참조 문서 추가
- 리포트에 섹션 추가 (기존 섹션은 그대로)
- 새 스킬 추가

`lookup_workplace.py` 추가가 정확히 이 경우입니다.
기존 사용법은 그대로이고 확인할 수 있는 것만 늘었습니다.

### PATCH (0.0.X) - 같은 일을 더 잘한다

- 오타, 표현 다듬기
- 판정 임계값 미세 조정 (부채비율 200%를 180%로)
- 스크립트 버그 수정
- 참조 문서 내용 보강

### 판단이 애매할 때

"이 변경 후에 사용자가 이전 결과물을 다시 봤을 때 혼란스러운가"를 묻습니다.
혼란스러우면 MAJOR, 아니면 MINOR 이하입니다.

임계값 조정은 특히 애매합니다.
같은 회사를 다시 조회했을 때 GREEN이 AMBER로 바뀔 수 있기 때문입니다.
**임계값을 바꿀 때는 PATCH라도 CHANGELOG에 판정이 달라질 수 있다고 반드시 적습니다.**

## 릴리스 절차

1. `CHANGELOG.md`의 `[Unreleased]` 항목을 새 버전 번호로 확정
2. 모든 `skills/*/SKILL.md`의 `metadata.version`을 일괄 갱신
3. 커밋 후 `git tag v0.2.0`
4. GitHub Release 생성. 릴리스 노트는 CHANGELOG 해당 절을 그대로 사용

버전이 4개 파일에 흩어져 있으므로 2번을 빠뜨리기 쉽습니다.
릴리스 전에 확인하세요.

```bash
grep -h "version:" skills/*/SKILL.md | sort -u
```

출력이 한 줄이 아니면 버전이 어긋난 것입니다.

## CHANGELOG 작성 원칙

개발자가 아니라 **사용자 시점**으로 씁니다.

```
나쁨: risk-signals.md의 Tier 2 판정 로직 리팩터링
좋음: 인원 감소 신호 판정이 국민연금 데이터 기준으로 바뀌었습니다.
      같은 회사를 다시 조회하면 이전과 다른 등급이 나올 수 있습니다.
```

결과가 달라질 수 있는 변경은 반드시 그 사실을 적습니다.
이 도구는 사람이 회사를 고르는 데 쓰이므로, 판정이 조용히 바뀌면 안 됩니다.

## 0.x 단계 정책

1.0.0 전까지는 MINOR에서도 호환성이 깨질 수 있습니다.
실제 API 응답으로 검증된 사례가 쌓이고 출력 형식이 안정되면 1.0.0을 답니다.
